interface:
  display_name: "AndroidProjectAgentsInstaller"
  short_description: "Install shared AI guidance docs into Android repos"
  default_prompt: "Use $AndroidProjectAgentsInstaller to install and tailor ProjectAgents guidance into this Android project."
